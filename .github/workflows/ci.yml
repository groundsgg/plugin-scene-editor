name: Java Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  reusable:
    uses: groundsgg/.github/.github/workflows/gradle-ci.yml@main
    secrets:
      PACKAGES_TOKEN: ${{ secrets.PACKAGES_TOKEN }}
